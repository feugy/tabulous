type Signal {
  from: ID!
  data: String!
}

input SignalInput {
  to: ID!
  data: String!
}

extend type Mutation {
  sendSignal(signal: SignalInput): Signal
}

type Subscription {
  awaitSignal(gameId: ID!): Signal
}
