mutation sendSignal($signal: SignalInput) {
  sendSignal(signal: $signal) {
    type
  }
}

subscription awaitSignal($gameId: ID) {
  awaitSignal(gameId: $gameId) {
    from
    type
    signal
  }
}
