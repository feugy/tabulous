/**
 * Base URL for game assets (with no traling slash).
 * @type {string}
 */
export const gameAssetsUrl = import.meta.env.WEB_GAME_ASSETS_URL

/**
 * Base URL for GraphQL endpoint (with no trailing slash).
 * @type {string}
 */
export const graphQlUrl = import.meta.env.WEB_GRAPHQL_URL
